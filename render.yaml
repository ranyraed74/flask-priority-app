
services:
  - type: web
    name: flask-prioritization-app
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python fix_flask_app.py"
    plan: free
    envVars:
      - key: PORT
        value: 5000
